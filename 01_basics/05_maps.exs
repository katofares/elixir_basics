# A map is a collection of key/value pairs. A map literal looks like this
# maps keys should be umique

# Ther are three syntax for map
# 01
foo_user = %{name: "foo", age: 37}

# 02
bar_user = %{:name => "bar", :age => 37}

# 03
baz_user = %{"name" => "baz", "age" => 37}
